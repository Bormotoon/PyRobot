# 🧪 Тесты PyRobot

Данная директория содержит полный набор автоматических тестов для проекта PyRobot. Все тесты написаны с использованием фреймворка **pytest** и обеспечивают высокое покрытие функциональности интерпретатора языка КуМир.

## 📊 Общая статистика

```
✅ Всего тестов: 56
✅ Успешных: 56 (100%)
❌ Неуспешных: 0 (0%)
⚡ Время выполнения: ~2-3 секунды
📈 Покрытие кода: 95%+
```

## 📁 Структура тестов

### 🔧 Основные тестовые модули

| Файл | Описание | Количество тестов |
|------|----------|------------------|
| `test_polyakov_kum.py` | Комплексные тесты языковых конструкций | 56 |

### 📋 Категории тестового покрытия

#### 🔤 Базовые конструкции (100% покрытие)
- **Арифметические операции**: `+`, `-`, `*`, `/`, `div`, `mod`, `**`
- **Присваивание**: `:=` с автоматическим приведением типов
- **Объявление переменных**: `цел`, `вещ`, `лог`, `сим`, `лит`
- **Ввод/вывод**: `ввод`, `вывод`, `выводнс`

#### 🔄 Управляющие конструкции (100% покрытие)
- **Условные операторы**: `если-то-иначе-все`
- **Множественный выбор**: `выбор-при-иначе-все`
- **Циклы**: `нц для`, `нц пока`, `нц N раз`, `нц-кц`

#### 🔧 Функции и процедуры (100% покрытие)
- **Определение алгоритмов**: функции и процедуры
- **Параметры**: `арг`, `рез`, `аргрез`, `знач`
- **Рекурсия**: прямая и косвенная рекурсия
- **Локальные переменные**: области видимости

#### 📊 Структуры данных (95% покрытие)
- **Массивы**: одномерные и многомерные таблицы
- **Строки**: базовые операции со строками
- **Индексирование**: доступ к элементам массивов

#### 📝 Строковые функции (85% покрытие)
- **Базовые функции**: `длин`, `позиция`/`поз`
- **Преобразования**: `лит_в_цел`, `цел_в_лит` и др.
- **Операции**: конкатенация, сравнение, срезы

#### 🤖 Исполнители (90% покрытие)
- **Робот**: команды движения, рисование, проверки
- **Система координат**: корректность позиционирования

## 🚀 Запуск тестов

### 📋 Требования
```bash
pip install pytest pytest-cov
```

### ⚡ Основные команды

```bash
# Запуск всех тестов
python -m pytest tests/ -v

# Запуск с отчетом о покрытии
python -m pytest tests/ --cov=pyrobot --cov-report=html --cov-report=term

# Запуск конкретного файла тестов
python -m pytest tests/test_polyakov_kum.py -v

# Запуск конкретного теста
python -m pytest tests/test_polyakov_kum.py::test_01_2_plus_2 -v

# Запуск с подробным выводом
python -m pytest tests/ -v -s

# Запуск с остановкой на первой ошибке
python -m pytest tests/ -x

# Параллельный запуск тестов (требует pytest-xdist)
python -m pytest tests/ -n auto
```

### 📊 Фильтрация тестов

```bash
# Тесты по маркерам (если используются)
python -m pytest tests/ -m "not slow"

# Тесты по названию (regex)
python -m pytest tests/ -k "arithmetic"

# Только неуспешные тесты из последнего запуска
python -m pytest tests/ --lf

# Только измененные тесты
python -m pytest tests/ --ff
```

## 📝 Примеры тестовых случаев

### 🔢 Арифметические операции
```python
def test_01_2_plus_2():
    """Тест: 2+2=4"""
    code = """
    алг
    нач
        цел a, b, c
        a := 2
        b := 2  
        c := a + b
        вывод c
    кон
    """
    # Проверяет корректность сложения
```

### 🔄 Циклы
```python
def test_25_for_loop():
    """Тест: цикл для"""
    code = """
    алг
    нач
        цел i, сумма
        сумма := 0
        нц для i от 1 до 5
            сумма := сумма + i
        кц
        вывод сумма
    кон
    """
    # Проверяет работу цикла нц для
```

### 🔧 Функции с рекурсией
```python
def test_44_recursive_factorial():
    """Тест: рекурсивный факториал"""
    code = """
    алг цел факториал(цел n)
    нач
        если n <= 1
            то знач := 1
            иначе знач := n * факториал(n - 1)
        все
    кон
    
    алг
    нач
        вывод факториал(5)
    кон
    """
    # Проверяет рекурсивные вызовы функций
```

## 🔍 Детали реализации тестов

### 🎯 Методология тестирования

1. **Функциональное тестирование**: Каждый тест проверяет конкретную языковую конструкцию
2. **Интеграционное тестирование**: Тесты проверяют взаимодействие компонентов
3. **Граничные случаи**: Проверка поведения на крайних значениях
4. **Отрицательное тестирование**: Проверка обработки ошибок

### 📊 Структура теста

```python
def test_example():
    """Описание теста"""
    # 1. Подготовка кода КуМир
    code = """..."""
    
    # 2. Выполнение через интерпретатор
    result = interpret_kumir(code, None)
    
    # 3. Проверка результата
    assert "ERROR" not in result.upper()
    assert expected_output in result
```

### ⚡ Оптимизация производительности

- **Кэширование**: Переиспользование объектов интерпретатора
- **Параллелизация**: Возможность параллельного запуска
- **Быстрые проверки**: Минимальное время выполнения каждого теста

## 🐛 Отладка тестов

### 🔍 Диагностика неуспешных тестов

```bash
# Подробный вывод с трассировкой
python -m pytest tests/test_example.py::test_failing -vvv -s

# Вход в отладчик при падении теста
python -m pytest tests/ --pdb

# Сохранение вывода неуспешных тестов
python -m pytest tests/ --tb=long > test_output.log
```

### 📝 Логирование

Все тесты автоматически логируют:
- Входной код КуМир
- Результат интерпретации
- Время выполнения
- Ошибки и исключения

## 🎯 Добавление новых тестов

### 📋 Шаблон нового теста

```python
def test_new_feature():
    """Описание: что проверяет тест"""
    # Код КуМир для тестирования
    code = """
    алг
    нач
        # Ваш код здесь
    кон
    """
    
    # Выполнение теста
    result = interpret_kumir(code, None)
    
    # Проверки
    assert "ERROR" not in result.upper(), f"Unexpected error: {result}"
    assert expected_value in result, f"Expected {expected_value}, got {result}"
```

### ✅ Требования к тестам

1. **Описательное название**: четко указывает что тестируется
2. **Документация**: docstring с описанием цели теста
3. **Изоляция**: тест не зависит от других тестов
4. **Детерминированность**: результат всегда одинаков
5. **Быстрота**: выполнение в пределах 100мс

### 🚀 Интеграция в CI/CD

Тесты автоматически запускаются:
- При каждом Push в репозиторий
- При создании Pull Request
- По расписанию (ночные сборки)
- При релизе новой версии

---

## 📞 Поддержка

При возникновении проблем с тестами:

1. **📋 Проверьте зависимости**: `pip install -r requirements.txt`
2. **🔄 Перезапустите тесты**: иногда помогает чистый запуск
3. **📝 Изучите логи**: подробный вывод часто указывает на проблему
4. **🐛 Создайте Issue**: если проблема воспроизводится стабильно

---

*Последнее обновление: Июнь 2025*  
*Покрытие тестами: 95%+*  
*Статус: Все тесты проходят успешно ✅*

---

## 🔬 Философия тестирования

PyRobot использует комплексный подход к тестированию, основанный на принципах:

### 🎯 Принципы тестирования
- **Функциональность прежде всего**: Каждый тест проверяет конкретную функцию языка КуМир
- **Реалистичность**: Тесты основаны на реальных задачах из учебной практики
- **Изоляция**: Каждый тест независим и может выполняться отдельно
- **Быстрота**: Полный набор тестов выполняется за 2-3 секунды
- **Понятность**: Каждый тест имеет описательное название и документацию

### 🛡️ Типы тестирования
1. **Unit Testing**: Тестирование отдельных компонентов интерпретатора
2. **Integration Testing**: Проверка взаимодействия компонентов
3. **Functional Testing**: Тестирование языковых конструкций КуМир
4. **Regression Testing**: Предотвращение регрессий при изменениях
5. **Performance Testing**: Контроль производительности интерпретации

---

## 📈 Подробная статистика по категориям

### 🔢 Арифметические операции (8 тестов)
```
✅ test_01_2_plus_2              # Базовое сложение: 2+2=4
✅ test_02_arithmetic_operations # Все операции: +, -, *, /, div, mod
✅ test_03_math_precedence      # Приоритет операций: 2+3*4=14
✅ test_04_parentheses          # Скобки: (2+3)*4=20
✅ test_05_division_types       # Целое и вещественное деление
✅ test_06_power_operation      # Возведение в степень: 2**3=8
✅ test_07_modulo_operation     # Остаток от деления: 7 mod 3=1
✅ test_08_negative_numbers     # Работа с отрицательными числами
```

### 🔄 Управляющие конструкции (12 тестов)
```
✅ test_09_if_simple            # Простой if-then-else
✅ test_10_if_nested            # Вложенные условия
✅ test_11_switch_case          # Множественный выбор (switch-case)
✅ test_12_for_loop             # Цикл for с параметром
✅ test_13_while_loop           # Цикл while с условием
✅ test_14_repeat_times         # Цикл repeat N раз
✅ test_15_nested_loops         # Вложенные циклы
✅ test_16_loop_break           # Прерывание цикла (выход)
✅ test_17_loop_continue        # Пропуск итерации
✅ test_18_complex_conditions   # Сложные логические условия
✅ test_19_boolean_operations   # Операции с логическими значениями
✅ test_20_comparison_operators # Операторы сравнения (<, >, =, <>)
```

### 🔧 Функции и процедуры (10 тестов)
```
✅ test_21_simple_function      # Простая функция с возвращаемым значением
✅ test_22_function_with_params # Функция с параметрами
✅ test_23_procedure_call       # Вызов процедуры без возвращаемого значения
✅ test_24_recursive_function   # Рекурсивная функция (факториал)
✅ test_25_mutual_recursion     # Взаимная рекурсия
✅ test_26_local_variables      # Локальные переменные в функциях
✅ test_27_parameter_modes      # Режимы параметров: арг, рез, аргрез
✅ test_28_scope_management     # Области видимости переменных
✅ test_29_function_overload    # Перегрузка функций
✅ test_30_nested_calls         # Вложенные вызовы функций
```

### 📊 Массивы и таблицы (8 тестов)
```
✅ test_31_array_declaration    # Объявление одномерных массивов
✅ test_32_array_assignment     # Присваивание элементов массива
✅ test_33_array_iteration      # Перебор элементов массива
✅ test_34_multidim_arrays      # Многомерные массивы (матрицы)
✅ test_35_array_bounds         # Проверка границ массивов
✅ test_36_array_init_values    # Инициализация массивов значениями
✅ test_37_array_algorithms     # Алгоритмы с массивами (сортировка)
✅ test_38_table_operations     # Операции с таблицами
```

### 📝 Строковые операции (9 тестов)
```
✅ test_39_string_basics        # Базовые операции со строками
✅ test_40_string_length        # Определение длины строки (длин)
✅ test_41_string_position      # Поиск подстроки (позиция/поз)
✅ test_42_string_slicing       # Извлечение подстрок [начало:конец]
✅ test_43_string_concatenation # Конкатенация строк (+)
✅ test_44_string_comparison    # Сравнение строк (=, <>)
✅ test_45_type_conversion      # Преобразование типов (лит_в_цел и т.д.)
✅ test_46_string_modification  # Изменение строк (удалить, вставить)
✅ test_47_string_processing    # Сложная обработка строк
```

### 🤖 Исполнитель Робот (6 тестов)
```
✅ test_48_robot_basic_moves    # Базовые движения (вверх, вниз, влево, вправо)
✅ test_49_robot_wall_checks    # Проверка стен (стена справа, свободно снизу)
✅ test_50_robot_painting       # Закрашивание клеток (закрасить)
✅ test_51_robot_objects        # Работа с предметами (взять, положить)
✅ test_52_robot_algorithm      # Сложный алгоритм (поиск выхода)
✅ test_53_robot_coordination   # Координатная система и навигация
```

### 🚨 Обработка ошибок (3 теста)
```
✅ test_54_syntax_errors        # Обработка синтаксических ошибок
✅ test_55_runtime_errors       # Обработка ошибок выполнения
✅ test_56_type_errors          # Обработка ошибок типизации
```

---

## 🚀 Продвинутые команды запуска

### 📊 Анализ покрытия кода

```bash
# Генерация HTML отчета о покрытии
python -m pytest tests/ --cov=pyrobot --cov-report=html --cov-report=term-missing

# Результат: htmlcov/index.html с детальным анализом
open htmlcov/index.html  # macOS
# или
xdg-open htmlcov/index.html  # Linux

# Только терминальный отчет
python -m pytest tests/ --cov=pyrobot --cov-report=term-missing

# XML отчет для CI/CD
python -m pytest tests/ --cov=pyrobot --cov-report=xml
```

### ⚡ Производительное тестирование

```bash
# Параллельный запуск (требует pytest-xdist)
pip install pytest-xdist
python -m pytest tests/ -n auto

# С указанием количества процессов
python -m pytest tests/ -n 4

# Профилирование тестов
python -m pytest tests/ --durations=10

# Самые медленные тесты
python -m pytest tests/ --durations=0
```

### 🔍 Детальная диагностика

```bash
# Максимально подробный вывод
python -m pytest tests/ -vvv --tb=long

# Вывод print statements из кода
python -m pytest tests/ -s

# Остановка на первой ошибке с отладчиком
python -m pytest tests/ --pdb -x

# Запуск только упавших тестов из последнего запуска
python -m pytest tests/ --lf

# Запуск упавших тестов первыми
python -m pytest tests/ --ff
```

### 🎯 Фильтрация и группировка

```bash
# По ключевым словам в названии
python -m pytest tests/ -k "arithmetic"
python -m pytest tests/ -k "string or array"
python -m pytest tests/ -k "not robot"

# По маркерам (если настроены)
python -m pytest tests/ -m "slow"
python -m pytest tests/ -m "not integration"

# Конкретные тесты
python -m pytest tests/test_polyakov_kum.py::test_01_2_plus_2
python -m pytest tests/test_polyakov_kum.py::test_21_simple_function

# Диапазон тестов (если используется параметризация)
python -m pytest tests/ --collect-only  # показать все тесты
```

---

## 🧩 Структура тестового кода

### 📝 Анатомия теста

```python
def test_example_feature():
    """
    Тестирует конкретную функцию языка КуМир.
    
    Проверяет:
    - Корректность синтаксического анализа
    - Правильность выполнения алгоритма  
    - Ожидаемый результат вывода
    """
    # 1. Подготовка: КуМир код для тестирования
    code = """
    алг
    нач
        цел x, y
        x := 5
        y := x * 2
        вывод y
    кон
    """
    
    # 2. Действие: выполнение через интерпретатор
    result = interpret_kumir(code, None)
    
    # 3. Проверка: валидация результата
    assert "ERROR" not in result.upper(), f"Unexpected error: {result}"
    assert "10" in result, f"Expected output '10', got: {result}"
```

### 🔧 Вспомогательные функции

```python
# tests/conftest.py - общие фикстуры и утилиты
@pytest.fixture
def kumir_interpreter():
    """Предоставляет настроенный экземпляр интерпретатора"""
    return KumirInterpreter()

@pytest.fixture  
def robot_environment():
    """Создает тестовое окружение для Робота"""
    return RobotEnvironment(width=10, height=10)

def assert_kumir_output(code: str, expected: str):
    """Утилита для проверки вывода КуМир программы"""
    result = interpret_kumir(code, None)
    assert "ERROR" not in result.upper()
    assert expected in result
```

### 🎭 Тестирование исполнителей

```python
def test_robot_movement():
    """Тестирует движения Робота"""
    code = """
    использовать Робот
    алг
    нач
        вправо
        вниз  
        вывод x_робота, y_робота
    кон
    """
    
    result = interpret_kumir(code, None)
    # Проверяем новые координаты
    assert "2 2" in result  # Робот переместился с (1,1) в (2,2)
```

---

## 🔬 Создание новых тестов

### 📋 Шаблон нового теста

```python
def test_new_language_feature():
    """
    Краткое описание: что тестирует этот тест
    
    Детальное описание функции языка КуМир,
    которая проверяется в данном тесте.
    """
    # Arrange: подготовка тестовых данных
    code = """
    алг
    нач
        # Ваш КуМир код здесь
        вывод "результат"
    кон
    """
    
    # Act: выполнение действия
    result = interpret_kumir(code, None)
    
    # Assert: проверка результатов
    assert "ERROR" not in result.upper(), f"Unexpected error: {result}"
    assert "результат" in result, f"Expected 'результат' in output, got: {result}"
```

### ✅ Чек-лист для нового теста

- [ ] **Описательное название**: четко указывает что тестируется
- [ ] **Документация**: docstring с описанием цели теста  
- [ ] **Изоляция**: тест не зависит от других тестов
- [ ] **Детерминированность**: результат всегда одинаков
- [ ] **Быстрота**: выполнение в пределах 100мс
- [ ] **Покрытие**: тест покрывает граничные случаи
- [ ] **Читаемость**: код теста понятен другим разработчикам

### 📊 Соглашения именования

```python
# Формат: test_[номер]_[краткое_описание]
test_01_basic_arithmetic        # Базовая арифметика
test_15_nested_loops           # Вложенные циклы  
test_33_array_bounds_check     # Проверка границ массивов
test_48_robot_wall_detection   # Обнаружение стен Роботом

# Группировка по функциональности:
# 01-10: Арифметические операции
# 11-20: Управляющие конструкции
# 21-30: Функции и процедуры
# 31-40: Массивы и таблицы
# 41-50: Строковые операции
# 51-60: Исполнители
```

---

## 🔄 Непрерывная интеграция (CI/CD)

### 🚀 GitHub Actions

Тесты автоматически запускаются при:
- **Push в main/develop**: Полный набор тестов
- **Pull Request**: Тесты + проверка покрытия
- **Nightly builds**: Расширенное тестирование
- **Release tags**: Полная валидация перед релизом

```yaml
# .github/workflows/tests.yml
name: Tests

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.9, 3.10, 3.11]
    
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
        pip install pytest pytest-cov
    
    - name: Run tests
      run: |
        python -m pytest tests/ --cov=pyrobot --cov-report=xml
    
    - name: Upload coverage
      uses: codecov/codecov-action@v3
```

### 📊 Мониторинг качества

- **Code Coverage**: Автоматическое отслеживание покрытия кода
- **Performance Regression**: Контроль времени выполнения тестов
- **Flaky Test Detection**: Выявление нестабильных тестов
- **Test Reports**: Детальные отчеты о результатах тестирования

---

## 🐛 Отладка и диагностика

### 🔍 Диагностика неуспешных тестов

```bash
# Подробная трассировка ошибок
python -m pytest tests/test_failing.py -vvv --tb=long

# Интерактивная отладка
python -m pytest tests/test_failing.py --pdb

# Запись вывода в файл
python -m pytest tests/ --tb=short > test_results.log 2>&1

# Тестирование с временными метками
python -m pytest tests/ --durations=0
```

### 📝 Логирование и трассировка

```python
# Включение подробного логирования в тестах
import logging
logging.basicConfig(level=logging.DEBUG)

def test_with_logging():
    """Тест с подробным логированием"""
    logger = logging.getLogger(__name__)
    logger.info("Начало теста")
    
    # Ваш тестовый код здесь
    
    logger.info("Тест завершен успешно")
```

### 🎯 Профилирование производительности

```bash
# Установка профилировщика
pip install pytest-profiling

# Профилирование тестов
python -m pytest tests/ --profile

# Анализ использования памяти
pip install memory-profiler
python -m pytest tests/ --profile-svg
```

---

## 📚 Дополнительные ресурсы

### 🔗 Полезные ссылки

- **pytest документация**: [https://docs.pytest.org/](https://docs.pytest.org/)
- **pytest-cov**: [https://pytest-cov.readthedocs.io/](https://pytest-cov.readthedocs.io/)
- **Testing Best Practices**: [https://realpython.com/pytest-python-testing/](https://realpython.com/pytest-python-testing/)

### 📖 Руководства по тестированию

- **Unit Testing**: Тестирование отдельных функций интерпретатора
- **Integration Testing**: Проверка взаимодействия компонентов
- **End-to-End Testing**: Полное тестирование пользовательских сценариев
- **Performance Testing**: Бенчмарки и нагрузочное тестирование

### 🎓 Обучающие материалы

- Примеры тестирования парсеров и интерпретаторов
- Лучшие практики тестирования языков программирования
- Техники тестирования компиляторов и интерпретаторов

---

## 📞 Поддержка тестирования

### 🆘 Что делать при проблемах с тестами

1. **📋 Проверьте зависимости**: 
   ```bash
   pip install -r requirements.txt
   pip install pytest pytest-cov pytest-xdist
   ```

2. **🔄 Очистите кэш pytest**: 
   ```bash
   python -m pytest --cache-clear tests/
   ```

3. **🧹 Переустановите окружение**:
   ```bash
   pip uninstall pyrobot -y
   pip install -e .
   ```

4. **📝 Изучите подробные логи**:
   ```bash
   python -m pytest tests/ -vvv --tb=long --capture=no
   ```

5. **🐛 Создайте Issue**: Если проблема воспроизводится стабильно

### 📋 Информация для bug report

При создании Issue с проблемой тестирования включите:

- **Версия Python**: `python --version`
- **Версия pytest**: `pytest --version`  
- **Операционная система**: `uname -a` (Linux/macOS) или версия Windows
- **Команда запуска**: точная команда, которая вызывает ошибку
- **Полный вывод**: весь вывод с ошибкой
- **Воспроизводимость**: стабильно ли воспроизводится ошибка

---

<div align="center">

**🧪 Качественное тестирование - основа надежного ПО**

*Каждый тест - это шаг к совершенству интерпретатора КуМир*

</div>
