#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Тестирование продвинутых режимов параметров (рез, аргрез) для пользовательских функций и процедур
"""

import sys
import os
sys.path.insert(0, os.path.join(os.path.dirname(__file__)))

from pyrobot.backend.kumir_interpreter.runtime_utils import interpret_kumir

def test_procedure_with_rez_parameter():
    """Тест процедуры с параметром 'рез' (только вывод)"""
    print("🧪 Тестирую процедуру с параметром 'рез'...")
    
    code = """
алг увеличить_на_один(арг цел x, рез цел результат)
нач
    результат := x + 1
кон

алг главный
нач
    цел a, b
    a := 5
    увеличить_на_один(a, b)
    вывод "Результат: ", b, нс
кон
"""
    
    try:
        result = interpret_kumir(code.strip())
        print("✅ Процедура с 'рез' параметром выполнена успешно!")
        print(f"Результат: {result}")
        return True
    except Exception as e:
        print(f"❌ Ошибка при выполнении процедуры с 'рез' параметром: {e}")
        return False

def test_procedure_with_argrez_parameter():
    """Тест процедуры с параметром 'аргрез' (ввод-вывод)"""
    print("🧪 Тестирую процедуру с параметром 'аргрез'...")
    
    code = """
алг удвоить(аргрез цел x)
нач
    x := x * 2
кон

алг главный
нач
    цел число
    число := 7
    вывод "До: ", число, нс
    удвоить(число)
    вывод "После: ", число, нс
кон
"""
    
    try:
        result = interpret_kumir(code.strip())
        print("✅ Процедура с 'аргрез' параметром выполнена успешно!")
        print(f"Результат: {result}")
        return True
    except Exception as e:
        print(f"❌ Ошибка при выполнении процедуры с 'аргрез' параметром: {e}")
        return False

def test_function_with_mixed_parameters():
    """Тест функции со смешанными режимами параметров"""
    print("🧪 Тестирую функцию со смешанными параметрами...")
    
    code = """
алг цел обработать(арг цел входное, рез цел дополнительное, аргрез цел изменяемое)
нач
    дополнительное := входное * 3
    изменяемое := изменяемое + 10
    знач := входное + изменяемое
кон

алг главный
нач
    цел x, y, z, результат
    x := 4
    y := 0  | будет заполнено через 'рез'
    z := 15 | будет изменено через 'аргрез'
    
    результат := обработать(x, y, z)
    
    вывод "Результат функции: ", результат, нс
    вывод "Дополнительное: ", y, нс
    вывод "Изменяемое: ", z, нс
кон
"""
    
    try:
        result = interpret_kumir(code.strip())
        print("✅ Функция со смешанными параметрами выполнена успешно!")
        print(f"Результат: {result}")
        return True
    except Exception as e:
        print(f"❌ Ошибка при выполнении функции со смешанными параметрами: {e}")
        return False

def test_multiple_procedures_with_references():
    """Тест нескольких процедур с параметрами по ссылке"""
    print("🧪 Тестирую несколько процедур с параметрами по ссылке...")
    
    code = """
алг инициализировать(рез цел x, рез цел y)
нач
    x := 10
    y := 20
кон

алг обменять(аргрез цел a, аргрез цел b)
нач
    цел временная
    временная := a
    a := b
    b := временная
кон

алг главный
нач
    цел первое, второе
    
    инициализировать(первое, второе)
    вывод "Инициализированные: ", первое, ", ", второе, нс
    
    обменять(первое, второе)
    вывод "После обмена: ", первое, ", ", второе, нс
кон
"""
    
    try:
        result = interpret_kumir(code.strip())
        print("✅ Несколько процедур с параметрами по ссылке выполнены успешно!")
        print(f"Результат: {result}")
        return True
    except Exception as e:
        print(f"❌ Ошибка при выполнении нескольких процедур с параметрами по ссылке: {e}")
        return False

def test_recursive_function_with_references():
    """Тест рекурсивной функции с параметрами по ссылке"""
    print("🧪 Тестирую рекурсивную функцию с параметрами по ссылке...")
    
    code = """
алг цел фибоначчи_подсчет(арг цел n, аргрез цел счетчик)
нач
    счетчик := счетчик + 1
    если n <= 1
        то знач := n
        иначе знач := фибоначчи_подсчет(n - 1, счетчик) + фибоначчи_подсчет(n - 2, счетчик)
    все
кон

алг главный
нач
    цел число, подсчеты, результат
    число := 6
    подсчеты := 0
    
    результат := фибоначчи_подсчет(число, подсчеты)
    вывод "Фибоначчи(", число, ") = ", результат, нс
    вывод "Количество вызовов: ", подсчеты, нс
кон
"""
    
    try:
        result = interpret_kumir(code.strip())
        print("✅ Рекурсивная функция с параметрами по ссылке выполнена успешно!")
        print(f"Результат: {result}")
        return True
    except Exception as e:
        print(f"❌ Ошибка при выполнении рекурсивной функции с параметрами по ссылке: {e}")
        return False

def main():
    """Основная функция тестирования"""
    print("=== ТЕСТИРОВАНИЕ ПРОДВИНУТЫХ РЕЖИМОВ ПАРАМЕТРОВ ===\n")
    
    tests = [
        test_procedure_with_rez_parameter,
        test_procedure_with_argrez_parameter,
        test_function_with_mixed_parameters,
        test_multiple_procedures_with_references,
        test_recursive_function_with_references
    ]
    
    passed = 0
    total = len(tests)
    
    for test in tests:
        try:
            if test():
                passed += 1
            print()
        except Exception as e:
            print(f"❌ Неожиданная ошибка в тесте {test.__name__}: {e}\n")
    
    print(f"=== РЕЗУЛЬТАТЫ ТЕСТИРОВАНИЯ ===")
    print(f"Пройдено: {passed}/{total}")
    
    if passed == total:
        print("🎉 ВСЕ ТЕСТЫ ПРОЙДЕНЫ! Продвинутые режимы параметров работают корректно!")
        return True
    else:
        print("❌ Некоторые тесты не прошли. Требуется дополнительная отладка.")
        return False

if __name__ == "__main__":
    main()
