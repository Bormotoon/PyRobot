<?xml version="1.0" encoding="UTF-8"?>
<kumir_course>
    <task>
        <USER_PRG testId="30" prg="алг цел найти максимум|@protected
дано | цел N, таб цел A[1:N] |@protected  
надо | найти максимальный элемент массива |@protected
нач |@protected
    цел max
    max := A[1]
    нц для i от 2 до N
        если A[i] &gt; max то
            max := A[i]
        всё
    кц
    знач := max
кон |@protected

алг цел @тестирование|@hidden
нач
    цел N
    N := 5
    таб цел A[1:N]
    A[1] := 3; A[2] := 7; A[3] := 2; A[4] := 9; A[5] := 1
    цел результат
    результат := найти максимум(N, A)
    вывод результат
кон"/>
    </task>
    
    <task>
        <USER_PRG testId="31" prg="алг цел найти минимум|@protected
дано | цел N, таб цел A[1:N] |@protected  
надо | найти минимальный элемент массива |@protected
нач |@protected
    цел min
    min := A[1]
    нц для i от 2 до N
        если A[i] &lt; min то
            min := A[i]
        всё
    кц
    знач := min
кон |@protected

алг цел @тестирование|@hidden
нач
    цел N
    N := 5
    таб цел A[1:N]
    A[1] := 3; A[2] := 7; A[3] := 2; A[4] := 9; A[5] := 1
    цел результат
    результат := найти минимум(N, A)
    вывод результат
кон"/>
    </task>
</kumir_course>
