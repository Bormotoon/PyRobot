#!/usr/bin/env python3
# -*- coding: utf-8 -*-

"""
Расширенный тест пользовательских функций
"""

from pyrobot.backend.kumir_interpreter.runtime_utils import interpret_kumir

def test_factorial_function():
    """Тестирует рекурсивную функцию факториала"""
    print("🧪 Тестирую рекурсивную функцию факториал...")
    
    code = """
алг цел факториал(арг цел n)
нач
  если n <= 1
    то знач := 1
    иначе знач := n * факториал(n - 1)
  все
кон

алг главный
нач
  вывод факториал(5)
кон
"""
    
    print("📄 Код для выполнения:")
    print(code)
    print("🚀 Запускаю интерпретацию...")
    
    result = interpret_kumir(code)
    
    print(f"✅ Выполнение завершено! Результат: {result.strip()}")
    print()

def test_string_function():
    """Тестирует функцию работы со строками"""
    print("🧪 Тестирую функцию работы со строками...")
    
    code = """
алг лит первый_символ(арг лит строка)
нач
  если длин(строка) > 0
    то знач := строка[1]
    иначе знач := ' '
  все
кон

алг главный
нач
  вывод первый_символ("Привет")
кон
"""
    
    print("📄 Код для выполнения:")
    print(code)
    print("🚀 Запускаю интерпретацию...")
    
    result = interpret_kumir(code)
    
    print(f"✅ Выполнение завершено! Результат: {result.strip()}")
    print()

def test_boolean_function():
    """Тестирует логическую функцию"""
    print("🧪 Тестирую логическую функцию...")
    
    code = """
алг лог четное(арг цел число)
нач
  знач := (число mod 2 = 0)
кон

алг главный
нач
  вывод четное(10)
  вывод четное(7)
кон
"""
    
    print("📄 Код для выполнения:")
    print(code)
    print("🚀 Запускаю интерпретацию...")
    
    result = interpret_kumir(code)
    
    print(f"✅ Выполнение завершено! Результат: {result.strip()}")
    print()

def test_multiple_parameters():
    """Тестирует функцию с несколькими параметрами"""
    print("🧪 Тестирую функцию с несколькими параметрами...")
    
    code = """
алг вещ среднее(арг цел a, арг цел b, арг цел c)
нач
  знач := (a + b + c) / 3.0
кон

алг главный
нач
  вывод среднее(10, 20, 30)
кон
"""
    
    print("📄 Код для выполнения:")
    print(code)
    print("🚀 Запускаю интерпретацию...")
    
    result = interpret_kumir(code)
    
    print(f"✅ Выполнение завершено! Результат: {result.strip()}")
    print()

if __name__ == "__main__":
    print("🚀 Запуск расширенных тестов пользовательских функций...\n")
    
    # Простая арифметическая функция (базовый тест)
    print("🧪 Тестирую простую арифметическую функцию...")
    code = """
алг цел сумма(арг цел x, арг цел y)
нач
  знач := x + y
кон

алг главный
нач
  вывод сумма(2, 3)
кон
"""
    result = interpret_kumir(code)
    print(f"✅ Результат: {result.strip()}\n")
    
    # Тестируем различные типы функций
    test_multiple_parameters()
    test_boolean_function()
    
    # Более сложные тесты (могут пока не работать)
    try:
        test_factorial_function()
    except Exception as e:
        print(f"❌ Рекурсивная функция пока не работает: {e}\n")
    
    try:    
        test_string_function()
    except Exception as e:
        print(f"❌ Строковая функция пока не работает: {e}\n")
    
    print("🎉 Расширенное тестирование завершено!")
