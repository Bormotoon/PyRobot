# 📝 СТРОКОВЫЕ ФУНКЦИИ КУМИР

## Статус: В РАБОТЕ / ЧАСТИЧНО РЕАЛИЗОВАНО

**Цель:** Реализовать строковые функции для прохождения тестов 46-54.

**Общая информация из документации КуМир:**

* **Исполнитель "Строки":** В "классических" версиях КуМир (для начальных и средних классов) многие алгоритмы работы со строками находятся в исполнителе `Строки`. Для их использования необходимо в начале программы написать: `использовать Строки`.
* **"Highgrade" КуМир:** В версиях КуМир для старших классов и олимпиад исполнитель `Строки` входит в базовую часть языка, и явное `использовать Строки` не требуется (но не является ошибкой).
* **Операции:**
  * Конкатенация (слияние): `строка1 + строка2`
  * Вырезка (слайс): `строка[начало:конец]`
  * Получение символа по индексу: `строка[индекс]`
  * Сравнение: `строка1 = строка2`, `строка1 <> строка2`

**Необходимые функции (из тестов и документации КуМир):**

1. **Базовые/Встроенные функции (не требуют `использовать Строки`):**
    * `длин(лит строка) -> цел`: Длина строки.
    * `код(сим c) -> цел`: Порядковый номер символа в таблице CP-1251.
    * `юникод(сим c) -> цел`: Порядковый номер символа в таблице Unicode.
    * `символ(цел n) -> сим`: Символ по номеру `n` из таблицы CP-1251.
    * `юнисимвол(цел n) -> сим`: Символ по номеру `n` из таблицы Unicode.

2. **Функции преобразования типов (не требуют `использовать Строки`):**
    * `цел_в_лит(цел число) -> лит`: Целое число в строку.
    * `вещ_в_лит(вещ число) -> лит`: Вещественное число в строку.
    * `лит_в_цел(лит строка, рез лог успех) -> цел`: Строку в целое число, `успех` = `да`/`нет`.
    * `лит_в_вещ(лит строка, рез лог успех) -> вещ`: Строку в вещественное число, `успех` = `да`/`нет`.
    * `Цел(лит строка, цел по умолчанию) -> цел`: Строку в целое, иначе значение по умолчанию (с большой буквы).
    * `Вещ(лит строка, вещ по умолчанию) -> вещ`: Строку в вещественное, иначе значение по умолчанию (с большой буквы).
    * `Лог(лит строка, лог по умолчанию) -> лог`: Строку ("да", "1", "истина", "нет", "0", "ложь") в логическое, иначе значение по умолчанию (с большой буквы).

3. **Функции исполнителя "Строки" (требуют `использовать Строки` в классическом КуМир):**
    * `верхний регистр(лит строка) -> лит`: Преобразование строки к верхнему регистру.
    * `нижний регистр(лит строка) -> лит`: Преобразование строки к нижнему регистру.
    * `позиция(лит фрагмент, лит строка) -> цел` (синоним: `поз`): Позиция первого символа подстроки (1-based, 0 если не найдено).
    * `позиция после(цел начало_поиска, лит фрагмент, лит строка) -> цел` (синоним: `поз после`): Поиск подстроки, начиная с позиции `начало_поиска`.
    * `вставить(лит фрагмент, аргрез лит строка, арг цел начало_вставки)`: Вставка `фрагмента` в `строку` с позиции `начало_вставки`.
    * `удалить(аргрез лит строка, арг цел начало_удаления, арг цел количество)`: Удаление `количества` символов из `строки` с позиции `начало_удаления`.
    * `заменить(аргрез лит строка, арг лит старый_фрагмент, арг лит новый_фрагмент, арг лог каждый)`: Замена `старого_фрагмента` на `новый_фрагмент`. Если `каждый` = `да`, то все вхождения, иначе первое.

**Текущее состояние реализации (в PyRobot, согласно предыдущим заметкам):**

* **✅ Реализовано (частично или полностью):**
  * `длин`
  * `позиция`/`поз` (базовый поиск)
  * Индексирование (`s[i]`) и срезы (`s[a:b]`) (операции языка)
  * `лит_в_цел`, `лит_в_вещ`, `цел_в_лит`, `вещ_в_лит` (базовые версии, проблема с `рез лог успех`)
  * `удалить`, `вставить`
  * Архитектура: хендлеры в `builtin_handlers.py`, реализация в `builtin_functions.py`, интеграция с `expression_evaluator`.
* **❌ Отсутствует / Требует доработки:**
  * **Функции из документации, не упомянутые ранее в плане PyRobot:**
    * `код`, `юникод`, `символ`, `юнисимвол`
    * `Цел`, `Вещ`, `Лог` (с большой буквы, с параметром по умолчанию)
    * `верхний регистр`, `нижний регистр`
    * `позиция после` / `поз после`
    * `заменить`
  * **Механизм `использовать Строки`:** Необходим для корректной работы функций модуля "Строки" и решения проблем с областью видимости (тест `51-str-proc.kum`).
  * **Обновление параметра `успех`** в функциях `лит_в_цел`/`лит_в_вещ`.
  * **Модификация символов через индексацию (`s[i] := 'x'`):** Не стандартная операция КуМир, в документации не описана как присваивание элементу строки. Модификация строк идет через `вставить`, `удалить`, `заменить`.
  * **Конкатенация строк и символов (`'строка' + 'с'`):** Должна работать, т.к. `сим` может автоматически преобразовываться в `лит`.
  * Недостаточное тестирование краевых случаев.

**Текущие ошибки в тестах (согласно предыдущим заметкам, с учетом новой информации):**

* **46-str-ab.kum:** Ошибка `KumirNameError: Переменная 'длин' не объявлена` указывает, что `длин` либо не зарегистрирована, либо проблема с ее вызовом.
* **47-str-ops.kum:** `KumirTypeError: Операция сложения не применима к типам 'ЛИТ' и 'СИМ'`. Проблема с реализацией конкатенации или автоматическим преобразованием типов.
* **48-str-search.kum:** Вероятно, `позиция` не найдена, проблема с параметрами или требует `использовать Строки`.
* **50-str-num.kum:** Функции преобразования (`лит_в_цел` и т.д.) не реализованы или работают некорректно с параметром `успех`.
* **51-str-proc.kum:** Проблемы с областью видимости функций модуля "Строки" (вероятно, из-за отсутствия или некорректной работы `использовать Строки`).
* **53-str-rec.kum:** Проблемы со строковой рекурсией (может быть связано с общей работой процедур/функций или спецификой строковых операций в них).

**План действий / реализации (обновленный):**

1. **Фундамент:**
    * Реализовать/доработать механизм `использовать Строки` для корректного подключения и области видимости функций этого модуля.
    * Обеспечить корректную конкатенацию строк с символами (автоматическое приведение `сим` к `лит`).
    * Доработать функции `лит_в_цел` и `лит_в_вещ` для корректной работы с параметром `рез лог успех`.
2. **Реализация функций (в соответствии с документацией):**
    * **Базовые/Встроенные:** `длин`, `код`, `юникод`, `символ`, `юнисимвол`.
    * **Преобразования:** `цел_в_лит`, `вещ_в_лит` (проверить существующие), `лит_в_цел`, `лит_в_вещ` (доработать `успех`), `Цел`, `Вещ`, `Лог`.
    * **Модуль "Строки":** `верхний регистр`, `нижний регистр`, `позиция`/`поз`, `позиция после`/`поз после`, `вставить`, `удалить` (проверить существующие), `заменить`.
3. **Операции языка:**
    * Проверить/доработать слайсы `s[a:b]` и получение символа `s[i]` в `expression_evaluator.py`.
    * *Примечание: Модификация `s[i] := 'x'` не является стандартной, вместо нее используются процедуры `вставить`/`удалить`/`заменить`.*

**Ключевые файлы для модификации:**

* `pyrobot/backend/kumir_interpreter/interpreter_components/builtin_handlers.py` (регистрация функций)
* `pyrobot/backend/kumir_interpreter/interpreter_components/builtin_functions.py` (реализация функций)
* `pyrobot/backend/kumir_interpreter/interpreter_components/expression_evaluator.py` (операции +, срезы, индексация)
* Файлы, отвечающие за обработку `использовать ИмяМодуля` и управление областями видимости модулей.

---

## 🏗️ АРХИТЕКТУРНЫЙ АНАЛИЗ ИНТЕРПРЕТАТОРА PyRobot

**Статус: ПРОВЕДЕН ПОЛНЫЙ АНАЛИЗ** (без внесения изменений на момент анализа)

**Анализированные компоненты (Общий размер: ~380KB кода):**
`main_visitor.py`, `procedure_manager.py`, `statement_handlers.py`, `expression_evaluator.py`, `scope_manager.py`, `kumir_datatypes.py`, `kumir_exceptions.py`, `utils.py`, `runtime_utils.py`, `builtin_functions.py`, `math_functions.py`, `io_handler.py`, `constants.py`, Грамматика ANTLR.

**Сильные стороны архитектуры:**

1. **Модульная структура с миксинами:** Четкое разделение ответственности (DeclarationVisitorMixin, StatementHandlerMixin, IOHandlerMixin).
2. **Профессиональная ANTLR4 грамматика:** Поддержка сложных имен, case-insensitive ключевых слов, приоритеты операций.
3. **Развитая система типов:** `KumirValue` (значение и тип), `KumirType` enum, `KumirTableVar` (многомерные массивы).
4. **Комплексная обработка исключений:** Иерархия `KumirExecutionError` с точной локализацией.
5. **Stack-based механизм функций:** Поддержка рекурсии, 'знач'.

**Критические проблемы и области для улучшения:**

1. 🚨 **Множественные debug `print` логи в Production коде (КРИТИЧЕСКАЯ):**
    * **Примеры:** `print(f"[DEBUG] ...", file=sys.stderr)` в `main_visitor.py`, `expression_evaluator.py`, `io_handler.py`; файловая отладка в `runtime_utils.py`.
    * **Влияние:** Замусоривание stderr, потенциальные проблемы с производительностью.
    * **Рекомендация:** Немедленно заменить на proper logging с уровнями.

2. 🔄 **Циклические импорты (СРЕДНЯЯ):**
    * **Пример:** `main_visitor` → `expression_evaluator` → `main_visitor` (через `TYPE_CHECKING`).
    * **Рекомендация:** Рефакторинг для вынесения общих интерфейсов.

3. 🎯 **Неконсистентная обработка ошибок (СРЕДНЯЯ):**
    * Смешанное использование 0-based и 1-based индексов строк.
    * Различные подходы к передаче контекста ошибок.
    * Неконсистентное использование `line_content` в ошибках.

4. 📝 **Множественные TODO и незавершенные реализации (СРЕДНЯЯ):**
    * `expression_evaluator.py`: `# TODO: Обработка массивов (LBRACK ... RBRACK)` (вероятно, устарело, см. фикс инициализации), `# TODO: Добавить остальные встроенные функции`.
    * `main_visitor.py`: `# TODO: Implement array literal handling` (вероятно, устарело).
    * Отсутствуют: `functions/` (упоминались в планах).

5. 🔒 **Проблема с изоляцией областей видимости для рекурсивных функций (ВЫСОКАЯ):**
    * Документированная проблема в `scope_manager.py`: потенциальные утечки переменных между рекурсивными вызовами.
    * *Примечание: Несмотря на это, основные тесты на рекурсию проходят, что указывает на работоспособность в типовых сценариях.*

6. 🏗️ **Избыточная сложность `procedure_manager.py` (СРЕДНЯЯ):**
    * Большой размер файла (72KB), сложная система наследования, дублирование логики.

7. 💾 **Неэффективное использование памяти для `KumirTableVar` (НИЗКАЯ):**
    * Хранение всех элементов в Python dict, нет оптимизации для разреженных или больших таблиц.

**Статистика качества кода и покрытие функциональности (на момент анализа):**

* **Сложность компонентов:** `expression_evaluator.py` (9/10), `procedure_manager.py` (9/10), `main_visitor.py` (8/10).
* **Покрытие:**
  * Базовые типы, арифметика, процедуры с параметрами, функции и рекурсия: 100%.
  * Массивы и таблицы: 85% (инициализация литералами была TODO, но позже исправлена).
  * Встроенные функции: 75% (базовые есть).
  * Обработка ошибок: 95%.

**Приоритезированные рекомендации:**

1. **Критический приоритет:**
    * Удалить все debug `print`-ы, заменить на logging.
    * Исправить циклические импорты.
    * Стандартизировать обработку ошибок.
2. **Высокий приоритет:**
    * Решить проблему с изоляцией областей видимости для рекурсивных функций в `scope_manager`.
    * Рефакторинг `procedure_manager.py`.
    * Завершить реализацию недостающих функций (особенно строковых).
3. **Средний/Низкий приоритет:**
    * Добавить недостающие встроенные функции, создать модульную структуру для них.
    * Оптимизировать `KumirTableVar`.
    * Добавить comprehensive unit тесты, документацию API, оптимизацию производительности.

**Итоговая оценка проекта (на момент анализа):**

* Архитектура: 8/10 (отличная модульность, но есть сложность)
* Функциональность: 9/10 (почти полная реализация Кумир)
* Качество кода: 6/10 (Debug код снижает оценку)
* Поддерживаемость: 6/10 (высокая сложность затрудняет)
* Стабильность: 8/10 (основной функционал надежен)
* **Общая: 7.4/10**

**Заключение анализа:**
PyRobot - серьезная и профессиональная разработка интерпретатора Кумир.

* **Главные достижения:** Полноценный ANTLR4 парсер, рабочая система типов, корректная обработка процедур/функций (включая рекурсию), comprehensive exception handling, модульная архитектура.
* **Критические задачи для Production:** Очистка debug кода, исправление циклических импортов, решение проблемы изоляции в рекурсивных функциях.
* **Задачи для дальнейшего развития:** Оптимизация производительности, улучшение документации, расширение функциональности (встроенные функции, массивы, курсы).

---

### 🏛️ АНАЛИЗ СТРУКТУРЫ ОРИГИНАЛЬНОГО КУМИРА (kumir2-master)

**Общая архитектура:**
Сложная система с компонентами: `src/kumir2-libs/` (базовые библиотеки), `src/plugins/` (модульная система), `src/app/` (исполняемые приложения), `share/` (ресурсы), `include/` (заголовочные файлы).

**Библиотеки системы (`kumir2-libs`):**

* **`stdlib/`**: `kcore.cpp`, `kmath.cpp` (случайные числа), `kfiles.cpp` (ввод/вывод, кодировки), `encodings.cpp`.
* Другие: `vm/` (виртуальная машина), `dataformats/`, `errormessages/`, `extensionsystem/`, `utils/`, `widgets/`.

**Система плагинов (`plugins`):**

* Основные: `kumiranalizer/`, `kumircodegenerator/`, `kumircoderun/`, `editor/`, `coregui/`.
* Специализированные: `llvmcodegenerator/`, `python3language/`, `coursemanager/`.

**Приложения (`app`):**

* IDE: `kumir2-ide/`, `kumir2-classic/`, `kumir2-teacher/`.
* CLI: `kumir2-bc/`, `kumir2-xrun/`, `kumir2-llvmc/`.

**Файловая система и ввод/вывод (`kfiles.cpp`):**

* Глобальные потоки `stdin`/`stdout`.
* Управление открытыми файлами (`std::deque<FileType>`).
* Буферизация консоли.
* Кодировки: CP866, CP1251, KOI8-R, UTF-8, UTF-16. Автоопределение локали (CP866 для Windows, UTF-8 для других).

**Математическая библиотека:**

* Кроссплатформенный генератор случайных чисел (`irand`, `rrand`).

**Анализатор языка (`kumiranalizer`):**

* `lexer.cpp`, `syntaxanalizer.cpp`, `analizer.cpp`, `statement.cpp`.

**Ключевые архитектурные решения оригинала:**

* **Модульность:** Четкое разделение, система расширений.
* **Кроссплатформенность:** Поддержка Windows, Linux, macOS, адаптация кодировок.
* **Расширяемость:** Плагинная архитектура, поддержка внешних исполнителей.

**Выводы для PyRobot (что можно улучшить, ориентируясь на оригинал):**

1. **Модульность:** Дальнейшее разделение на логические библиотеки/модули.
2. **Кодировки:** Расширенная и явная поддержка различных кодировок.
3. **Файловая система:** Более детальная реализация файловых операций.
4. **Буферизация ввода/вывода:** Рассмотреть более продвинутую буферизацию.
5. **Плагины/Расширения:** Подумать о системе расширений для исполнителей (если применимо к целям PyRobot).

**Общий вывод:** Анализ оригинального КуМира подтверждает, что PyRobot захватывает основные принципы языка, хотя и в более упрощенной форме, что соответствует целям веб-ориентированного проекта.

---

## 🚀 РЕАЛИЗАЦИЯ СИСТЕМЫ КУРСОВ (ДЕТАЛЬНЫЙ ПЛАН)

**Статус: ПЛАНИРОВАНИЕ**

Этот план основан на предварительном анализе папки `kumir2-master/src/plugins/coursemanager/` и файлов `course_model.h`, `course_model.cpp`, `coursemanager_plugin.h`, `coursemanager_plugin.cpp`.

### Детальный анализ `coursemanager` из `kumir2-master`

**1. Основные компоненты:**

*   **`courseModel` (`course_model.h`, `course_model.cpp`):**
    *   Центральный компонент, управляющий данными курса.
    *   **Формат курса:** XML. Файл курса (например, `.kurs.xml`) содержит структуру: разделы, задания, описания, ссылки на файлы и т.д.
    *   **Структура XML (основные теги):**
        *   Корневой элемент: `<COURSE>` или `<WORKBOOK>`.
        *   Задания/уроки/задачи: `<T>` (Task). Атрибуты: `id` (уникальный идентификатор), `name` (название).
        *   Внутри `<T>`:
            *   `<DESC>`: Описание/текст задания для ученика.
            *   `<PROGRAM>`: Путь к файлу с начальным кодом или примером (`.kum`).
            *   `<CS>`: Используемый язык/исполнитель (например, "Кумир", "Робот").
            *   `<ISP>` (Исполнитель): Специфичные настройки для исполнителя.
                *   Атрибут `ispname` (например, "Robot", "VODOLLEY", "Чертежник").
                *   `<ENV>`: Путь к файлу обстановки (например, `.fil` для Робота). Может быть несколько для разных тестов одного задания.
                *   `<CONTROL>`: Ссылка на скрипт для более сложной проверки (детали требуют уточнения).
            *   `<CHECK>`: Текстовое описание критериев проверки или ожидаемого результата.
            *   `<MARK>`: Оценка за задание (число, обычно 0-10; специальные значения, например, 12 для "недоступно из-за зависимостей").
            *   `<DEPEND>`: Описывает зависимости от других заданий.
                *   `<ID>`: `id` задания, от которого есть зависимость.
                *   `<MIN>`, `<MAX>`: Минимальная/максимальная требуемая оценка по зависимому заданию.
            *   `<USER_PRG prg="...">`: Сохраненный код ученика.
            *   `<TESTED_PRG prg="...">`: Код ученика, успешно прошедший проверку.
        *   **Иерархия:** Теги `<T>` могут быть вложенными, формируя разделы и подразделы курса.
    *   **Работа с XML:** Используется `QDomDocument` (из Qt) для парсинга и манипуляций с XML-структурой.
    *   **Кэширование:** Присутствует кэш (`cash` в `course_model.cpp`) для быстрого доступа к XML-узлам по их `id`.
    *   **Модель для Qt View:** Класс `courseModel` наследуется от `QAbstractItemModel`, что позволяет отображать структуру курса в виде дерева в GUI КУМИРа.
    *   **Иконки и отображение:** Значительная часть кода связана с выбором и отображением иконок в GUI в зависимости от статуса задания (выполнено, доступно, оценка и т.д.).

*   **`Plugin` (`coursemanager_plugin.h`, `coursemanager_plugin.cpp`):**
    *   Плагин КУМИРа, интегрирующий `courseModel` в основное приложение.
    *   **Функции:**
        *   Загрузка файлов курсов (`.kurs.xml`) и "рабочих тетрадей" (файлы с расширениями `.rabota`, `.тетрадь`, `.kumir-workbook`).
        *   Взаимодействие с GUI КУМИРа (создание меню "Практикум", обработка действий пользователя).
        *   Инициализация и запуск проверки заданий:
            *   Получение кода ученика из редактора или сохраненных данных.
            *   Загрузка необходимых обстановок для исполнителей.
            *   Вызов стандартных компонентов КУМИРа: анализатора, генератора кода, исполнителя.
            *   Получение результата выполнения (обычно числовая оценка).
            *   Сохранение оценки и кода ученика в модели и, потенциально, в файле рабочей тетради.
        *   Поддержка пакетного режима (запуск из командной строки для автоматической проверки).
        *   Управление списком "недавних" файлов курсов.

*   **`MainWindowTask` (`task/coursemanager_window.h`, `task/coursemanager_window.cpp`, `task/coursemanager_window.ui`):**
    *   Отвечает за главное окно интерфейса для работы с курсами в GUI КУМИРа. Отображает дерево курса, текст задания, элементы управления. Его прямая реализация для PyRobot не требуется, но логика взаимодействия с `courseModel` и отображаемые элементы могут быть полезны при проектировании своего UI.

*   **Структура "Рабочей тетради" (Workbook):**
    *   Это также XML-файл, который обычно ссылается на основной файл курса.
    *   **Назначение:** Хранит данные, специфичные для ученика:
        *   `<FILE fileName="path/to/course.kurs.xml" />`: Ссылка на основной файл курса.
        *   `<MARK testId="..." mark="..." />`: Сохраненные оценки за задания. `testId` соответствует `id` тега `<T>` в файле курса.
        *   `<USER_PRG testId="..." prg="..." />`: Код, написанный учеником для конкретного задания.
        *   `<TESTED_PRG testId="..." prg="..." />`: Код, который успешно прошел проверку.
    *   **Процесс загрузки:** Сначала загружается основной файл курса, затем на его структуру "накладываются" данные (оценки, код) из файла рабочей тетради.

**2. Ключевые концепции и механики:**

*   **Задания (Tasks):** Атомарные учебные единицы курса. Каждое задание имеет описание, может иметь начальный код, требует определенных исполнителей с их обстановками и имеет механизм проверки.
*   **Исполнители (ISP):** Задания привязаны к конкретным исполнителям (например, "Робот", "Чертежник"). Для каждого исполнителя могут быть свои файлы обстановок.
*   **Обстановки (Environments/Fields):** Файлы (`.fil`, `.dat` и т.п.), описывающие начальное состояние мира для исполнителей. Одно задание может использовать несколько обстановок для проведения различных тестов.
*   **Проверка (Checking):** Процесс оценки решения ученика. Может включать:
    *   Запуск кода ученика.
    *   Сравнение результата (вывода программы, конечного состояния исполнителя) с эталоном.
    *   Возврат числовой оценки.
*   **Оценки (Marks):** Числовые значения, отражающие успешность выполнения. Используются для отслеживания прогресса и для механизма зависимостей.
*   **Зависимости (Dependencies):** Механизм, позволяющий открывать доступ к одним заданиям только после успешного выполнения других (с определенной оценкой).
*   **Сохранение прогресса:** Код, написанный учеником, и его оценки сохраняются, обычно в файле рабочей тетради.

**3. Что важно для PyRobot (Адаптация и реализация):**

*   **Структура данных курса (Python-классы):** Необходимо определить набор Python-классов, которые будут адекватно представлять курс, раздел, задание, требуемого исполнителя, файл обстановки, зависимость и т.д. Структура XML-файлов КУМИРа является хорошей отправной точкой.
*   **Формат файла курса:**
    *   **Вариант 1 (XML, совместимость):** Адаптировать XML-формат КУМИРа.
        *   *Плюсы:* Потенциальная возможность использовать существующие файлы курсов.
        *   *Минусы:* Работа с XML в Python может быть менее удобной, чем с JSON или YAML. Зависимость от `QDomDocument` отсутствует.
    *   **Вариант 2 (JSON/YAML, современность):** Разработать собственный формат на основе JSON или YAML.
        *   *Плюсы:* Легкость парсинга и генерации в Python, человекочитаемость.
        *   *Минусы:* Потребуется конвертер для существующих XML-курсов или их ручная адаптация.
    *   *Рекомендация:* Начать с поддержки XML для обеспечения базовой совместимости, но внутренне представлять данные в удобных Python-объектах. В будущем можно добавить поддержку JSON/YAML и конвертеры.
*   **Механизм загрузки курса:** Реализовать функцию или класс, который будет парсить файл курса (XML) и создавать из него объектную модель на Python.
*   **Управление состоянием ученика (аналог "Рабочей тетради"):**
    *   Определить способ хранения кода ученика и его оценок. Это может быть отдельный файл (например, JSON или XML) или, для веб-приложения, хранение в базе данных на сервере или в `localStorage` на клиенте.
*   **Интеграция с интерпретатором PyRobot:**
    *   При выборе задания из курса интерпретатор должен:
        *   Получить описание задания и начальный код.
        *   Настроиться на использование нужных исполнителей.
        *   Загрузить соответствующие файлы обстановок для этих исполнителей.
    *   При проверке задания:
        *   Выполнить код ученика.
        *   Сравнить результат с ожидаемым (если применимо).
        *   Вычислить и сохранить оценку.
*   **Обработка файлов обстановок:** Интерпретатор PyRobot (и его компоненты-исполнители) должен уметь корректно загружать и использовать файлы обстановок (`.fil`, `.dat` и т.д.).

### Подробный план внедрения системы курсов в PyRobot

**Этап 1: Базовая структура данных и загрузка курса (XML)**

1.  **Определить Python-классы для модели курса:**
    *   `Course`: Корневой объект. Атрибуты: `name`, `description`, `filePath`, список корневых `Task`.
    *   `Task`: Задание или раздел. Атрибуты: `id`, `name`, `description_text`, `initial_code_file_path` (относительный или абсолютный путь к `.kum`), `check_text`, список `RequiredExecutor`, список `Dependency`, список `SubTask` (для разделов). Пользовательские данные: `user_code: str`, `mark: int`, `is_completed: bool`.
    *   `RequiredExecutor`: Информация о необходимом исполнителе. Атрибуты: `name` (например, "Робот"), список `EnvironmentFile`, `control_script_file_path`.
    *   `EnvironmentFile`: Информация о файле обстановки. Атрибуты: `path` (относительный или абсолютный).
    *   `Dependency`: Зависимость от другого задания. Атрибуты: `dependent_task_id: str`, `min_mark: int`, `max_mark: int`.
    *   *Примечание: Эти классы должны максимально точно отражать структуру XML-файлов КУМИРа для обеспечения совместимости.*

2.  **Реализовать XML-парсер для файлов курсов (например, `.kurs.xml`):**
    *   Использовать стандартную библиотеку Python `xml.etree.ElementTree`.
    *   Создать функцию `load_course_from_xml(file_path: str) -> Course`.
        *   Эта функция будет читать XML-файл, рекурсивно обходить его структуру.
        *   Создавать экземпляры Python-классов (`Course`, `Task`, и т.д.), заполняя их данными из XML-тегов и атрибутов.
        *   Особое внимание уделить корректной обработке путей к файлам (обстановки, начальный код), которые часто бывают относительными к расположению файла курса. Учесть возможные проблемы с разделителями пути (Windows `\` vs Unix `/`).

3.  **Реализовать загрузку данных из "Рабочей тетради" (например, `.rabota`):**
    *   Создать функцию `apply_workbook_data_to_course(course_object: Course, workbook_file_path: str)`:
        *   Парсит XML-файл рабочей тетради.
        *   Для каждого элемента `<MARK>` и `<USER_PRG>`/`<TESTED_PRG>` находит соответствующий объект `Task` в `course_object` по `testId` (который соответствует `Task.id`).
        *   Обновляет поля `user_code` и `mark` в найденных объектах `Task`.

4.  **Начальное тестирование парсеров:**
    *   Использовать примеры файлов `.kurs.xml` и `.rabota` из `polyakov_kurs_work/` и `kumir2-master/courses/practicum/`.
    *   Написать тестовые скрипты, которые загружают эти файлы и выводят структуру созданных Python-объектов в консоль для проверки корректности парсинга.

**Этап 2: Интеграция с интерпретатором PyRobot**

1.  **Модификация основного класса интерпретатора (`KumirInterpreter` или аналогичного):**
    *   Добавить метод `prepare_for_task(task: Task, environment_index: int = 0)`:
        *   Вызывается при выборе учеником задания (и конкретного теста/обстановки, если их несколько).
        *   Интерпретатор получает информацию о необходимых исполнителях и файле обстановки (из `task.required_executors[i].environment_files[environment_index]`).
        *   Настраивает своих внутренних исполнителей (Робот, Чертежник и т.д.), загружая в них состояние из указанного файла обстановки. Это может потребовать доработки существующих классов исполнителей в PyRobot, чтобы они могли инициализироваться из файлов.
    *   Предоставить интерпретатору начальный код из `task.initial_code_file_path` (если есть) или `task.user_code` (если задание уже выполнялось).

2.  **Механизм проверки и оценки:**
    *   **Простой вариант:** Если программа ученика сама должна вернуть оценку (например, через специальную команду вывода `вывод оценка_число`), интерпретатор должен уметь это значение перехватить.
    *   **Сложный вариант (на будущее):** Если проверка требует анализа конечного состояния исполнителя (например, положение Робота, содержимое файла) или запуска внешнего скрипта (аналог тега `<CONTROL>` в КУМИРе), это потребует более сложной инфраструктуры.
        *   Для сравнения состояния исполнителя: нужен способ "сериализовать" или получить ключевые параметры состояния исполнителя после выполнения программы и сравнить их с эталонными.
    *   После выполнения и проверки, результат (оценка) должен сохраняться в `task.mark`, а `task.is_completed` обновляться.

3.  **Сохранение прогресса ученика:**
    *   Реализовать функцию `save_workbook_data(course_object: Course, workbook_file_path: str)`:
        *   Собирает актуальные `user_code` и `mark` из всех объектов `Task` в `course_object`.
        *   Генерирует XML-структуру, аналогичную файлу рабочей тетради КУМИРа, и сохраняет её в `workbook_file_path`.
    *   *Для веб-версии PyRobot этот механизм будет заменен на сохранение данных в серверной базе данных или в клиентском хранилище (`localStorage`).*

**Этап 3: Пользовательский интерфейс (минимальный, для разработки и тестирования)**

*   Даже если полноценный графический интерфейс не является первоочередной задачей, для отладки и демонстрации потребуется базовый UI:
    *   Возможность выбрать файл курса (`.kurs.xml`) и, опционально, файл рабочей тетради (`.rabota`).
    *   Отображение иерархического списка заданий из загруженного курса.
    *   Возможность выбрать задание для выполнения.
    *   Отображение текста (описания) выбранного задания.
    *   Простое текстовое поле для ввода/редактирования кода программы ученика.
    *   Кнопка "Проверить задание".
    *   Отображение результата проверки: полученной оценки, сообщений об ошибках.
*   Этот интерфейс может быть реализован как консольное приложение, либо как очень простое веб-приложение (например, с использованием Flask или FastAPI для бэкенда и минимального HTML/JS для фронтенда).

**Этап 4: Расширенные возможности и улучшения (на перспективу)**

1.  **Поддержка альтернативных форматов курсов (JSON/YAML):**
    *   Разработать четкую JSON-схему или YAML-структуру для описания курсов.
    *   Реализовать парсеры для этих форматов.
    *   Создать утилиту-конвертер из XML-формата КУМИРа в новый формат.

2.  **Продвинутая логика проверки заданий:**
    *   Реализация поддержки аналогов `<CONTROL>` скриптов, если это будет необходимо для существующих курсов.
    *   Разработка механизмов для сравнения состояния исполнителей с эталонными состояниями.

3.  **Управление зависимостями в UI:**
    *   В пользовательском интерфейсе визуально отмечать или делать недоступными задания, для которых не выполнены предварительные условия (зависимости).

4.  **Визуализация обстановок:**
    *   Если PyRobot будет включать графическое представление для исполнителей (например, поле Робота), оно должно уметь отображать текущую обстановку, загруженную из файла курса.

5.  **Режим учителя (Course Editor):**
    *   Разработка интерфейса для визуального создания и редактирования курсов, заданий, обстановок. Это значительная по объему задача.

### Ключевые файлы для дальнейшего изучения в `kumir2-master/src/plugins/coursemanager/`

*   `task/coursemanager_window.ui`: Файл описания интерфейса для Qt Designer. Поможет понять, какая информация и как именно представлена пользователю в GUI КУМИРа.
*   `task/editdialog.h`, `task/editdialog.cpp`, `task/editdialog.ui`: Компоненты диалога для редактирования свойств задания (используется в режиме учителя).
*   `task/newkursdialog.h`, `task/newkursdialog.cpp`, `task/newkursdialog.ui`: Компоненты диалога для создания нового курса (также для режима учителя).

Этот детальный план должен помочь нам системно подойти к реализации поддержки курсов.

---

## 🎯 ГЕНЕРАЦИЯ ТЕСТОВ PYTEST ИЗ ФАЙЛОВ КУРСОВ (ВМЕСТО ПОЛНОЙ СИСТЕМЫ КУРСОВ)

**Статус: ПЛАНИРОВАНИЕ**

**Приоритет: ВЫСОКИЙ** (Новая задача от пользователя)

**Цель:** Автоматически создать тесты `pytest` для каждого курса в папке `polyakov_kurs_work/`. Тесты будут основаны на заданиях из `.kurs.xml` файлов и эталонных решениях из соответствующих `.work.xml` файлов. Это позволит проверить интерпретатор на широком наборе задач.

**Предыдущая задача "РЕАЛИЗАЦИЯ СИСТЕМЫ КУРСОВ" отложена.**

**Детальный план генерации тестов:**

1.  **Итерация по курсам:**
    *   Просканировать директорию `c:\Users\Bormotoon\VSCodeProjects\PyRobot\polyakov_kurs_work\` для обнаружения поддиректорий. Каждая поддиректория представляет отдельный курс (например, `arrkum`, `c2kum`, `funkum`, `robokum`, `strkum`, `vodoley`).

2.  **Для каждой директории курса:**
    a.  **Идентификация файлов:**
        *   Найти главный файл курса: `[имя_курса].kurs.xml` (например, `arrkum/Массивы.kurs.xml`). Имя курса извлекается из имени директории.
        *   Найти файл с решениями. Шаблон имени может быть `Поляков_[имя_курса].work.xml` (как в примере `arrkum/Поляков_Массивы.work.xml`) или более общий, например, `*work*.xml` или `*workbook*.xml`. Необходимо будет уточнить или сделать поиск более гибким.
    b.  **Парсинг файла `.kurs.xml`:**
        *   Использовать библиотеку `xml.etree.ElementTree` для парсинга.
        *   Извлечь список всех заданий (элементы `<T>`). Для каждого задания сохранить:
            *   `id`: Атрибут `id` или `xml:id` тега `<T>`.
            *   `name`: Атрибут `name` или `xml:name` тега `<T>`.
            *   (Опционально) `description`: Содержимое тега `<DESC>`.
    c.  **Парсинг файла `.work.xml` (или аналогичного файла с решениями):**
        *   Убедиться, что файл ссылается на правильный `.kurs.xml` через тег `<FILE fileName="..."/>`.
        *   Извлечь эталонные решения. Они находятся в тегах `<USER_PRG>` или `<TESTED_PRГ>`.
            *   Атрибут `testId` связывает решение с `id` задания из `.kurs.xml`.
            *   Атрибут `prg` содержит полный код КуМир-программы для теста.
        *   Создать словарь: `solutions = {task_id: solution_code}`. Отдавать предпочтение коду из `<TESTED_PRГ>`, если он доступен для данного `testId`, иначе использовать `<USER_PRГ>`.
    d.  **Генерация файла теста `pytest`:**
        *   Имя файла: `test_[имя_курса].py` (например, `tests/test_arrkum.py`). Файл создается в директории `c:\Users\Bormotoon\VSCodeProjects\PyRobot\tests\`.
        *   Добавить в начало файла импорты:
            ```python
            import pytest
            from pyrobot.backend.kumir_interpreter.main_visitor import KumirInterpreter # Уточнить путь!
            import re # Для обработки кода
            ```
    e.  **Создание тестовых функций:**
        *   Для каждого задания, у которого есть `id` в `.kurs.xml` и соответствующий код решения в словаре `solutions`:
            *   **Имя функции:** `def test_task_[id_задания]_[нормализованное_имя_задания]():`.
                *   Нормализация имени: привести к нижнему регистру, заменить пробелы и недопустимые символы на `_`. Обеспечить уникальность.
            *   **Тело функции:**
                1.  Получить код решения (`solution_code_raw`) из словаря `solutions`.
                2.  **Предобработка кода КуМира:**
                    *   `decoded_code = solution_code_raw.replace('&#xa;', '\n').replace('&lt;', '<').replace('&gt;', '>').replace('&amp;', '&')` (и другие XML-сущности при необходимости).
                    *   `processed_code = re.sub(r'\|@protected|\|@hidden|\|@editable', '', decoded_code)` # Удаление маркеров IDE КуМира. Нужно проверить, не ломает ли это код, если маркеры могут быть частью строковых литералов (маловероятно для этих конкретных маркеров).
                3.  Создать экземпляр интерпретатора: `interpreter = KumirInterpreter()`.
                4.  Выполнить код: `execution_result = interpreter.execute(processed_code)`.
                    *   Предполагается, что `interpreter.execute()` возвращает объект (например, dataclass `ExecutionResult`) со следующими полями:
                        *   `stdout: str`
                        *   `stderr: str`
                        *   `error: Exception | None` (ошибка времени выполнения Python, возникшая в интерпретаторе)
                        *   `kumir_error: bool` (флаг, если сам КуМир код сообщил об ошибке, например, через вывод "Ошибка деления на ноль")
                        *   `return_value: any` (если применимо, значение функции `@тестирование`)
                5.  **Ассерты `pytest`:**
                    *   `assert execution_result.error is None, f"Python runtime error in interpreter: {execution_result.stderr}"`
                    *   `assert "Задание зачтено." in execution_result.stdout, f"Task {task_id} ({task_name}) failed: 'Задание зачтено.' not in stdout. Stdout: {execution_result.stdout}"`
                    *   `assert "Программа работает неверно!" not in execution_result.stdout, f"Task {task_id} ({task_name}) explicitly reported an error in stdout: {execution_result.stdout}"`
                    *   `assert "Ошибка!" not in execution_result.stdout.lower(), f"Task {task_id} ({task_name}) might have failed, 'Ошибка!' in stdout: {execution_result.stdout}"` (общий поиск слова "ошибка")
                    *   `# TODO: Если @тестирование возвращает значение (например, 10), можно добавить ассерт на него, если execution_result.return_value его содержит.`

3.  **Задачи для реализации:**
    *   Написать Python-скрипт (или набор функций), который будет выполнять шаги 1 и 2.
    *   Определить точный API `KumirInterpreter().execute()` и структуру возвращаемого им результата. Если текущий API не предоставляет достаточно информации (например, `return_value` от КуМир-функции), его, возможно, потребуется доработать.
    *   Начать реализацию с одного курса (например, `arrkum`) для отладки.

**Пример обработки кода из `prg` атрибута:**
Код из `prg` содержит как сам алгоритм решения, так и тестирующую обвязку (например, `алг цел @тестирование`). Интерпретатор должен выполнить весь этот код как единое целое. Тестирующая обвязка внутри КуМир-кода сама вызовет нужные алгоритмы и произведет проверки, выводя результат в `stdout`.

**Открытые вопросы и возможные сложности:**
*   **Точный формат имени файла с решениями:** Нужно подтвердить, всегда ли это `Поляков_[имя_курса].work.xml` или есть вариации.
*   **Обработка маркеров `|@protected`, `|@hidden`:** Простое удаление может быть не всегда корректным, если они влияют на парсинг комментариев или многострочных строк (хотя в КуМире это маловероятно).
*   **API интерпретатора:** Ключевой момент – как `KumirInterpreter().execute()` возвращает результаты. Если он просто печатает в `sys.stdout`, потребуется перехват вывода (`io.StringIO`). Идеально, если он возвращает структурированный объект.
*   **Кодировки файлов:** Убедиться, что XML-файлы читаются в правильной кодировке (вероятно, UTF-8).
*   **Пути к файлам обстановок:** Некоторые задания могут использовать внешние файлы обстановок (`.fil` для Робота и т.д.), на которые ссылается `.kurs.xml`. Текущий план не учитывает их загрузку в интерпретатор, так как `.work.xml` обычно содержит самодостаточный код для проверки. Если тесты из `.work.xml` все же полагаются на внешние файлы, это усложнит задачу.

**Завершение:**
После генерации всех тестовых файлов, можно будет запустить `python -m pytest tests/` для проверки всего набора.
